# from django.conf.urls.defaults import *

# urlpatterns = patterns('sources.views',
    # (r'^$','index'),
	# (r'^source_query$','source_query'),
	# (r'^add$','add'),
	# (r'^add_submit$','add_submit'),
    # (r'^(?P<source_id>\d+)/$', 'detail'),
	# (r'^(?P<source_id>\d+)/edit$', 'edit'),
	# (r'^(?P<source_id>\d+)/edit_submit$', 'edit_submit'),
	# (r'^(?P<source_id>\d+)/delete$', 'delete'),
	# (r'^(?P<source_id>\d+)/delete_submit$', 'delete_submit'),
# )
